import { getWorkspaceData } from '@/lib/data';
import { labels } from '@/lib/format';
export async function GET(){const data=await getWorkspaceData();const escape=(v:unknown)=>`"${String(v??'').replace(/^[=+@-]/,"'$&").replaceAll('"','""')}"`;const rows=[['LAPORAN OPERASIONAL',data.settings.companyName],['Tanggal',new Date().toLocaleDateString('id-ID')],[],['ARMADA'],['Kode Unit','Kategori','Merek / Model','Status','Lokasi','Tarif per Jam (Rp)'],...data.fleet.map(f=>[f.unitCode,f.category,f.brandModel,labels[f.status],f.currentLocation,f.hourlyRate]),[],['PENAGIHAN'],['Nomor Tagihan','Tanggal Terbit','Jatuh Tempo','Total (Rp)','PPN (Rp)','Status'],...data.invoices.map(i=>[i.invoiceNumber,i.issueDate,i.dueDate,i.totalAmount,i.taxAmount,labels[i.status]])];return new Response('\uFEFF'+rows.map(r=>r.map(escape).join(';')).join('\r\n'),{headers:{'Content-Type':'text/csv; charset=utf-8','Content-Disposition':`attachment; filename="laporan-operasional-${new Date().toISOString().slice(0,10)}.csv"`,'Cache-Control':'no-store'}});}
