{
  "name": "rentalin-media-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Rentalin Media API — authorization, validasi, presigned URL R2, protected read (docs/media-architecture.md).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
